---
title: "Control Flow in Rust: if, loop, while, and for"
sidebarTitle: "Control Flow"
description: "Use if expressions, loop, while, and for in Rust to direct program flow. Includes returning values from loops and iterating over collections."
mode: "wide"
---

Control flow lets you run different code depending on conditions or repeat code while a condition holds true. Rust provides `if` expressions, `loop`, `while`, and `for` loops for this purpose.

<Note>This chapter is coming soon. Refer to the [official Rust Book chapter on control flow](https://doc.rust-lang.org/stable/book/ch03-05-control-flow.html) in the meantime.</Note>
